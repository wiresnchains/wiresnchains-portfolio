# portfolio
My personal portfolio made for college assignment

## Deployment
Use the `npm run build` command to build the react app and **remember to remove the source map `.map` files from production!**
